import React from "react"
import { AsciIcon, AsciIconProps } from "../asci-icon"

export const SettingsIcon = (props: Omit<AsciIconProps, "name">) => <AsciIcon name="settings" {...props} />
export const ShieldIcon = (props: Omit<AsciIconProps, "name">) => <AsciIcon name="shield" {...props} />
export const ShieldCheckIcon = (props: Omit<AsciIconProps, "name">) => <AsciIcon name="shieldCheck" {...props} />
export const CheckCircleIcon = (props: Omit<AsciIconProps, "name">) => <AsciIcon name="checkCircle" {...props} />
export const AlertCircleIcon = (props: Omit<AsciIconProps, "name">) => <AsciIcon name="alert" {...props} />
export const LockIcon = (props: Omit<AsciIconProps, "name">) => <AsciIcon name="lock" {...props} />
export const SlidersIcon = (props: Omit<AsciIconProps, "name">) => <AsciIcon name="sliders" {...props} />
